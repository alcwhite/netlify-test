[build]
command = "npm run build && echo $CONTEXT > netlify-context"

[context.branch-deploy]
command = """
npm run build && echo $CONTEXT > netlify-context && echo -e "/*
  Basic-Auth: ${PREVIEW_LOGIN}:${PREVIEW_PASSWORD}" > _site/_headers"""

[context.deploy-preview]
command = """
npm run build && echo $CONTEXT > netlify-context && echo -e "/*
  Basic-Auth: ${PREVIEW_LOGIN}:${PREVIEW_PASSWORD}" > _site/_headers"""
